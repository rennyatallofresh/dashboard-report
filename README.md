# dashboard-report

AlloFresh CS dashboard built with React and Vite. The app consumes the public Google Apps Script JSON endpoint directly from the browser and can be deployed as a static site.

## Commands

```bash
npm install
npm run dev
npm run build
```
